import styled from 'styled-components';

export const PlayerOverlay = styled.article`
	position: fixed;
	bottom: 0;
	left: 0;
	width: 100%;
	height: 20%;
	min-height: 12rem;
	background-color: red;
`;
